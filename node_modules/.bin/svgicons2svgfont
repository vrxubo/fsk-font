#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../svgicons2svgfont/bin/svgicons2svgfont.js" "$@"
  ret=$?
else 
  node  "$basedir/../svgicons2svgfont/bin/svgicons2svgfont.js" "$@"
  ret=$?
fi
exit $ret
